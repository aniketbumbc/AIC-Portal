import { Injectable } from '@angular/core';

const routes = {
  users: '/users'
};

@Injectable({
  providedIn: 'root'
})
export class UserService {

  constructor() { }

  getAll() {
    // this.
  }
}
